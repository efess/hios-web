# Base URL for the API backend.
# Empty = use relative URLs (works with Vite dev proxy to localhost:3000)
# Set to a full URL to point at a different API server.
VITE_API_BASE_URL=
# VITE_API_BASE_URL=http://192.168.1.50:3000
