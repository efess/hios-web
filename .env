# Base URL for the API backend. Leave empty to use relative URLs (works with Vite proxy in dev).
# Set to a full URL (e.g. http://192.168.1.50:3000) to point at a remote API.
VITE_API_BASE_URL=http://localhost:3000
